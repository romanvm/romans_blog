branches:
  only:
    - master
    - develop
language: python
python:
  - "3.4"
  - "3.5"
addons:
  apt:
    packages:
     - enchant
before_install: pip install codecov
install: pip install -r requirements.txt
script: coverage run manage.py test blog pages
after_success:
 - python publish_docs.py
 - codecov
